name: river_byok
description: Credential-safe BYOK provider connections for AI, TTS and podcast transcription.
publish_to: none
version: 0.1.0
resolution: workspace
environment:
  sdk: '>=3.6.0 <4.0.0'
dependencies:
  crypto: 3.0.7
  http: 1.6.0
  river_ai:
    path: ../river_ai
  river_audio:
    path: ../river_audio
dev_dependencies:
  river_domain:
    path: ../river_domain
  test: ^1.25.0
