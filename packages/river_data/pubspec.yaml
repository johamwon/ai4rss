name: river_data
description: Drift/SQLite persistence and durable jobs for River.
publish_to: none
version: 0.1.0
resolution: workspace
environment:
  sdk: '>=3.10.0 <4.0.0'
dependencies:
  drift: 2.33.0
  river_feed:
    path: ../river_feed
  river_domain:
    path: ../river_domain
dev_dependencies:
  build_runner: ^2.7.1
  drift_dev: 2.34.0
  test: ^1.31.0
