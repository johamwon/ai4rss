name: river_sync
description: Local-first merge and synchronization rules.
publish_to: none
version: 0.1.0
resolution: workspace
environment:
  sdk: '>=3.6.0 <4.0.0'
dependencies:
  cryptography: 2.9.0
dev_dependencies:
  test: ^1.25.0
