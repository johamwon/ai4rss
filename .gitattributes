* text=auto eol=lf

*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# Preserve vendored upstream sources byte-for-byte; upstream contains legacy
# whitespace that should not weaken checks for River-owned files.
third_party/file_picker/** -text -whitespace
